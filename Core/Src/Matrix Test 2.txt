  /* USER CODE END 2 */

  GPIOB->ODR |= (1<<0);
 // GPIOB->ODR &=~(1<<0);
  /* Infinite loop */
  /* USER CODE BEGIN WHILE */
  while (1)
  {
    /* USER CODE END WHILE */
	  GPIOB->ODR |= (1<<0);
  	  GPIOC->ODR &=~(1<<0);
  	        for (volatile uint32_t n = 0; n < 912357; n++);
  	        //      HAL_GPIO_WritePin(LED_GREEN_GPIO_Port, LED_GREEN_Pin, GPIO_PIN_SET);
	  GPIOC->ODR |= (1<<0);
	  for (volatile uint32_t n = 0; n < 912357; n++);
	      //  HAL_GPIO_WritePin(LED_GREEN_GPIO_Port, LED_GREEN_Pin, GPIO_PIN_RESET);
  	  GPIOC->ODR &=~(1<<1);
  	        for (volatile uint32_t n = 0; n < 912357; n++);
  	        //      HAL_GPIO_WritePin(LED_GREEN_GPIO_Port, LED_GREEN_Pin, GPIO_PIN_SET);
	  GPIOC->ODR |= (1<<1);
	  for (volatile uint32_t n = 0; n < 912357; n++);
	      //  HAL_GPIO_WritePin(LED_GREEN_GPIO_Port, LED_GREEN_Pin, GPIO_PIN_RESET);
  	  GPIOC->ODR &=~(1<<2);
  	        for (volatile uint32_t n = 0; n < 912357; n++);
  	        //      HAL_GPIO_WritePin(LED_GREEN_GPIO_Port, LED_GREEN_Pin, GPIO_PIN_SET);
	  GPIOC->ODR |= (1<<2);
	  for (volatile uint32_t n = 0; n < 912357; n++);
	      //  HAL_GPIO_WritePin(LED_GREEN_GPIO_Port, LED_GREEN_Pin, GPIO_PIN_RESET);
  	  GPIOC->ODR &=~(1<<3);
  	        for (volatile uint32_t n = 0; n < 912357; n++);
  	        //      HAL_GPIO_WritePin(LED_GREEN_GPIO_Port, LED_GREEN_Pin, GPIO_PIN_SET);
	  GPIOC->ODR |= (1<<3);
	  for (volatile uint32_t n = 0; n < 912357; n++);
	      //  HAL_GPIO_WritePin(LED_GREEN_GPIO_Port, LED_GREEN_Pin, GPIO_PIN_RESET);
  	  GPIOC->ODR &=~(1<<4);
  	        for (volatile uint32_t n = 0; n < 912357; n++);
  	        //      HAL_GPIO_WritePin(LED_GREEN_GPIO_Port, LED_GREEN_Pin, GPIO_PIN_SET);
	  GPIOC->ODR |= (1<<4);
	  for (volatile uint32_t n = 0; n < 912357; n++);
	      //  HAL_GPIO_WritePin(LED_GREEN_GPIO_Port, LED_GREEN_Pin, GPIO_PIN_RESET);
  	  GPIOC->ODR &=~(1<<5);
  	        for (volatile uint32_t n = 0; n < 912357; n++);
  	        //      HAL_GPIO_WritePin(LED_GREEN_GPIO_Port, LED_GREEN_Pin, GPIO_PIN_SET);
	  GPIOC->ODR |= (1<<5);
	  for (volatile uint32_t n = 0; n < 912357; n++);
	      //  HAL_GPIO_WritePin(LED_GREEN_GPIO_Port, LED_GREEN_Pin, GPIO_PIN_RESET);
  	  GPIOC->ODR &=~(1<<6);
  	        for (volatile uint32_t n = 0; n < 912357; n++);
  	        //      HAL_GPIO_WritePin(LED_GREEN_GPIO_Port, LED_GREEN_Pin, GPIO_PIN_SET);
	  GPIOC->ODR |= (1<<6);
	  for (volatile uint32_t n = 0; n < 912357; n++);
	      //  HAL_GPIO_WritePin(LED_GREEN_GPIO_Port, LED_GREEN_Pin, GPIO_PIN_RESET);
  	  GPIOC->ODR &=~(1<<7);
  	        for (volatile uint32_t n = 0; n < 912357; n++);
  	        //      HAL_GPIO_WritePin(LED_GREEN_GPIO_Port, LED_GREEN_Pin, GPIO_PIN_SET);
	  GPIOC->ODR |= (1<<7);
	  for (volatile uint32_t n = 0; n < 912357; n++);
	      //  HAL_GPIO_WritePin(LED_GREEN_GPIO_Port, LED_GREEN_Pin, GPIO_PIN_RESET);


    /* USER CODE BEGIN 3 */
  }